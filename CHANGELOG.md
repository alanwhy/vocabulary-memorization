# Changelog

本项目的版本变更记录，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [1.16.1] - 2026-08-31

### Fixed
- 修复闪卡翻页仍会短暂露出下一张中文释义的问题：点击「忘记/记住」后改为先翻回正面、等 0.5s 翻转动画结束再切下一张，不再与切换同时进行

## [1.16.0] - 2026-08-31

### Added
- 例句、近义词、反义词、形近词里的单词支持点击查询：弹出浮层展示该词的释义、音标、读音（豆包语音）与例句；若该词不在个人词库中则自动录入一次（新增 `GET /api/words/lookup` 精确查词接口）
- 闪卡单词面右上角显示背诵次数徽标（复用 6 档次数配色）

### Changed
- 例句、近义词、反义词、形近词不再按「词库命中」给所有词上色，改为只高亮当前单词本身；点击查询取代原「命中高亮」作为生词跳转入口，高亮配色沿用蓝→紫冷色调与次数徽标热力色区分
- 闪卡队列排序把「背诵次数」提升为第一优先级：每组（含每天第一批 30 张）优先取背诵次数最多且未归档的单词，直到「记住」归档；新词仅在同次数时靠前
- 归档后的单词再次录入后，归档按钮禁用并显示「已归档」，避免误操作报错

### Fixed
- 修复闪卡翻页释义泄露：点击「忘记/记住」切下一张时先无动画翻回正面再切换，避免 0.5s 翻转动画期间露出下一张卡片的背面中文释义

## [1.15.0] - 2026-08-30

### Added
- 单词读音：接入豆包（火山引擎）语音合成，查词成功后后台预生成发音 mp3 到磁盘（`audio/` 目录，docker 挂 volume 持久化），单词卡与闪卡正面新增喇叭按钮点读

### Changed
- 豆包语音为唯一读音来源：未配置或合成失败时前端明确提示原因（如「语音合成未配置」「语音合成失败: request and grant appid mismatch」），不再回退浏览器本地合成
- 后台管理页新增「豆包语音配置」（API Key / Cluster / 音色），与 DeepSeek 配置同页管理；后端 `/api/admin/settings` 接口扩展为两者平铺结构

## [1.14.0] - 2026-08-28

### Added
- 闪卡复习时，对「有正常释义但缺扩展信息（例句/词根词缀/近反义/形近词）」的词，点「不认识」会自动后台触发一次查词补全，无需用户手动重试

### Changed
- 闪卡队列排序提高背诵次数权重：到期词从「按到期时间先后」改为「按背诵次数降序优先」，反复背反复忘的词优先出现；新词仍排最前，「当天已复习过」的词仍由间隔重复排期保证当天不再出现

## [1.13.0] - 2026-08-27

### Added
- 查词拼写校验：DeepSeek 先判断单词拼写是否正确，拼写错误时在释义中显示「error：请检查单词拼写是否正常」而非空释义
- 用户侧对「无释义 / 查询失败 / 拼写错误」的单词改为显示「重试」按钮（不显示归档），点击重新触发一次查词（新增 `POST /api/words/{id}/retry`）；重试按钮对所有用户（含管理员）生效，并识别旧版「系统提示」占位
- 单词查询进行中时，在单词右侧展示「查询中...」状态，查询结束自动隐藏
- 后台词库管理支持多条词条同时「重新查询」，不再一次只允许一条
- 后台用户管理支持禁用/启用用户与删除用户：禁用后该用户登录被拒、已有会话立即失效；删除需二次确认并连带清理其单词记录（新增 `POST /api/admin/users/{id}/disable`、`DELETE /api/admin/users/{id}`）

### Changed
- 用户表新增 `disabled` 列（幂等迁移）；查询失败/拼写错误的占位释义 pos 统一为 `error`，前端据此渲染红色提示与重试按钮
- 移除启动期「清理无释义单词」逻辑：无释义/错误单词现在保留在用户列表里供主动重试，不再于重启时被自动删除
- 背单词页输入框与「添加」按钮改用 Element Plus 默认尺寸组件，替换原手写的大号样式
- 后台用户管理、词库管理的表格操作列固定在右侧（sticky），列多时横向滚动而不是折行

### Fixed
- 修复后台管理页样式泄漏到背单词页（全局选择器 `.sense`/`button.primary` 等命中单词卡，进后台再返回会导致删除按钮变大）：后台样式统一收窄到 `.admin-page` 作用域
- 修复统计页首次切换卡顿：echarts + echarts-wordcloud 懒加载 chunk 约 1MB，改为登录后空闲时预加载，点进去不再等下载

## [1.12.0] - 2026-08-26

### Added
- 接口鉴权由 Cookie 改为 Bearer Token：登录返回 `{ token, user }`，前端把 token 存 localStorage 并在每次请求带 `Authorization: Bearer` 头；未登录访问数据接口返回 401
- 个人中心「重置次数」按钮（放在「修改密码」右侧）：二次确认后调用 `POST /api/words/reset-counts`，把当前用户所有单词的 `review_count` 重置为 1（不动闪卡复习排期）

### Changed
- 管理侧删除词库单词（单条/批量）时，同步删除所有用户已保存的同名单词
- 启动迁移新增「清理无释义单词」：删除用户侧 `senses` 为空或为「查询失败」占位、且不在查词中的单词

### Fixed
- 修复前端仍按每页 100 条请求、未真正生效 1.11.0 声明的 20 条分页的问题
- 修复管理端「导出 CSV」在 Bearer 鉴权下失效（由 `<a href>` 改为 fetch + Blob 下载）

## [1.11.0] - 2026-08-25

### Added
- 查词信息强化：DeepSeek 查词一并返回音标、例句（含中文翻译）、词根词缀、近义词、反义词、形近词，近反义/形近词均带中文释义
- 词库词高亮：例句、近义词、反义词、形近词中命中全局词库的词，按出现次数分级高亮（复用 6 档配色）；新增 `GET /api/vocabulary` 返回词库索引 `[{word_key, occurrence_count}]`
- 录入/再次录入时自动补全缺失的强化字段：以「形近词」字段是否存在判定数据是否已由最新 prompt 强化，缺则后台补全、齐则不重复调用大模型

### Changed
- 单词卡布局重排：次数与日期移到单词行最右侧，归档/删除操作移到卡片底部，按钮改用 Element 主题色（归档）/删除色（删除）
- 词根词缀/近义词/反义词/形近词的标题加粗加深，具体内容保持浅色
- 分页默认每页 100 条改为 20 条

## [1.10.0] - 2026-08-19

### Added
- 后台 DeepSeek 配置新增「兜底模型」「思考模型」两个字段：兜底模型作为查词默认模型，思考模型仅做配置预留（后续再启用）
- 统计页新增「近 7 天词云」（按累计背诵次数加权）与「开头字母统计」（按 `display_word` 首字母分组）两张图
- 词库管理顶部展示全库单词总数（不受筛选影响，`total_all`）

### Changed
- 查词链路删除 Google 免费翻译兜底，默认只用兜底模型查询，失败仅走重试、不再降级到在线接口
- 统计页所有图表（14 天趋势、背诵分布、词云、字母统计）改用 echarts 实现，替换原 CSS 手绘

### Fixed
- 修复词库管理「导出 CSV」按钮与工具栏搜索框/下拉/批量删除按钮上下不对齐的问题

## [1.9.0] - 2026-08-14

### Added
- 后台词库管理新增「暂无释义 / 已有释义」状态筛选，可快速筛出释义为空的词条：`GET /api/admin/dictionary` 新增 `status` 参数，`senses` 是否为空在数据库侧判断
- 后台词库管理对「暂无释义」词条新增「重新查询」按钮，管理员可手动触发一次查词并写回全局缓存，用于修复接口超时导致的假阴性（新增 `POST /api/admin/dictionary/retry`）

### Fixed
- 查词重试预算从 6 次收紧到 3 次，缩短新词停留在「查询中」的最长等待时间；彻底失败后不再留空，改为写入「查询失败，请管理员重试」占位提示——只写用户自己的释义，不污染全局词库缓存，避免后续用户命中错误缓存
- 新增 `words.translation_started_at` 列与周期扫描，自愈后台查词 goroutine 在写回结果前意外退出导致的永久卡死；此前这类单词会一直显示「查询中」直到进程重启

## [1.8.1] - 2026-08-12

### Fixed
- 首页「今日背诵次数」计算错误：原 SQL `SUM(review_count)` 会把各单词的累计历史背诵次数全部加进来（一个累计背了 10 次的单词今天背 1 次却被计为 10 次），且时间范围只有 `>= 零点` 缺少上界 `< 次日零点`。现改为对当天窗口内被复习过的每个单词计数 1，并精确划定 `[00:00:00, 23:59:59)` 窗口

## [1.8.0] - 2026-08-11

### Added
- 首页顶部 stats 旁新增「今日背诵次数」，由后端按 `last_reviewed_at >= 本地零点` 聚合 `review_count` 之和算出；删除单词时同步扣减
- 后台词库管理搜索框同时支持单词和释义模糊匹配：后端在 `word_key` 与 `senses[].translation` 上分别做 LIKE/JSON_SEARCH 查询，命中即返回
- 后台词库管理支持批量删除：表格新增多选列与表头全选框，「批量删除（N）」按钮一键删除选中的词库缓存；新增 `POST /api/admin/dictionary/batch-delete`，单次最多 200 条

### Fixed
- 按时间排序时输入已存在的单词不会被置顶：原逻辑只在新增单词时插到列表最前，累加次数分支只做原地替换，导致「按时间」排序下用户看不到顺序变化。现按当前排序模式就地调整（按时间移到最顶、按次数按当前次数重新插入、按字母原地替换）

## [1.7.0] - 2026-08-07

### Added
- 深色/浅色主题手动切换：顶栏新增切换按钮，选择结果存本地缓存，不跟随系统偏好；首次访问固定浅色，刷新不闪屏。Element Plus 组件同步换肤
- 单词列表改为后端分页 + 滚动加载（每页 100 条），排序（按次数/时间/字母）改由后端执行，翻页不再出现跨页重复或漏词；归档页、后台词库管理表同样改为分页，词库过滤改为后端关键字查询
- 新增 `GET /api/stats` 聚合接口：统计页的总词汇量、累计背诵次数、次数分布、14 天新增趋势全部由 SQL 聚合算出，不再下载全量单词
- 单词卡的背诵次数徽标按次数分 6 档配色（边界 1/3/5/7/9/11），色相从浅绿经黄、橙推到红与深红，次数越多越醒目；后台词库管理表新增「出现次数」列并复用同一套配色
- 记录用户最后登录时间：个人中心显示「最后登录时间」和「录入单词数」（含已归档），管理员用户列表新增「最后登录」「录入单词数」两列
- 新增 `GET /api/words/translating`：查词中的单词改为按 id 增量补齐，不再整表重载，查词完成时页面滚动位置不再跳动

### Fixed
- 统计页头部四张卡片宽高不一致：`.card + .card` 的全局上边距命中了网格内第 2/3/4 张卡片，把它们整体下推了 16px；现已在网格内抵消，并统一为等宽等高（窄屏 2×2）
- 删除统计页底部「最久未复习」模块

### Changed
- 数据库迁移新增 `users.last_login_at` 列与 `words(user_id, archived)` 索引，启动时幂等补齐
- 前端收敛重复代码：时间格式化统一到 `utils/format.js`，分页状态机与滚动加载抽为 `usePaginatedList` / `useInfiniteScroll`

## [1.6.0] - 2026-08-04

### Security
- 修改密码后清除除当前登录设备外的所有其他会话；管理员重置用户密码后清除该用户全部会话
- 登录、改密码接口新增失败次数限流（15 分钟内失败 5 次锁定 15 分钟），防止暴力破解
- Session Cookie 支持通过 `COOKIE_SECURE` 环境变量按需开启 `Secure` 属性
- 管理员重置密码、删除/归档单词等接口在目标不存在时正确返回 404，不再误报成功

### Changed
- 后端重构为 repository + 依赖注入分层（`store.go`/`app.go`），handler 不再直接操作全局数据库连接，便于测试和维护；新增 29 项单元测试与 handler 级测试，覆盖鉴权、限流、单词/词典等核心逻辑
- HTTP 服务增加读写/空闲超时与优雅关闭：收到关闭信号后等待在途请求和后台查词任务完成再退出
- 数据库连接池按环境变量可配置（`DB_MAX_OPEN_CONNS`/`DB_MAX_IDLE_CONNS`），SQL 调用改为带 context 版本，支持超时中断
- 后台查词任务改为受信号量限流（默认最多 5 个并发，`MAX_CONCURRENT_TRANSLATIONS` 可配置）、可被优雅关闭取消，并在进程重启后自动重新触发上次未完成的查词任务
- 新增全局 panic 恢复中间件，单个请求内的异常不再影响其他请求

## [1.5.1] - 2026-08-04

### Changed
- 前端主题色由绿色改为 Element Plus 默认主色（`#409eff`），浅色/深色模式的辅助底色同步对齐 Element Plus 官方取值，不再和组件库内部默认的蓝色浅底不一致

## [1.5.0] - 2026-08-04

### Changed
- 前端从 `backend/static/` 下 5 个各自独立、CDN 引入 Vue3 的静态页面，重写为 `frontend/` 目录下的 Vue 3 + Vite 单页应用：引入 Vue Router（history 模式，`/profile`、`/archive`、`/stats`、`/admin` 均为无 `.html` 后缀的干净路径）和 Pinia 集中管理鉴权状态，消除了原来 topbar、单词列表、鉴权样板、fetch 错误处理等大量跨页面重复代码
- `backend/Dockerfile` 改为多阶段构建，新增 Node 构建阶段编译前端产物；`docker-compose.yml` 构建上下文调整为仓库根目录以让 Docker 构建看到 `frontend/`；`docker compose up -d --build` 仍是唯一部署命令，行为不变
- `backend/main.go` 新增 SPA 深链接回退，直接访问 `/profile` 等路径并刷新不再 404

## [1.4.0] - 2026-08-04

### Added
- 管理员后台支持重置指定用户的密码，重置后该用户所有已登录会话立即失效，需重新登录
- 管理员后台"词库管理"支持删除词库缓存记录（仅清除全局缓存，不影响用户已保存的单词释义）

## [1.3.0] - 2026-08-04

### Added
- 新增全局词库缓存：同一个单词无论被哪个用户输入，释义只向大模型查询一次，之后全站复用
- 查词失败后按退避间隔（2s/4s/8s/16s/30s）自动重试，不再永久留空释义
- 同一词性的多条释义自动合并成一条，用中文分号分隔
- 管理员后台新增"词库管理"，可查看词库中每个单词的出现次数与最后更新时间

## [1.2.0] - 2026-08-04

### Added
- 单词支持归档/取消归档，新增归档页面 `/archive.html`

## [1.1.0] - 2026-08-04

### Added
- 单词列表支持按次数 / 时间（最近复习时间） / 字母三种排序方式，选择结果本地缓存（localStorage）
- 输入框旁新增"添加"按钮，功能与回车一致；提交成功后输入框自动重新聚焦
- 登录页新增联系方式，忘记密码或需要开通账号可联系 alanwhy0528@gmail.com
- 页面底部新增版本号展示

## [1.0.0] - 之前

- 首个上线版本：单词记录、DeepSeek/在线翻译查词、多用户登录、管理员后台、统计页面
