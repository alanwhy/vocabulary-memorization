# 安全政策（Security Policy）

## 支持版本

本仓库只有一个主线分支（`main`），持续维护。安全修复只落在当前最新版本。

## 上报漏洞

如果你发现了安全漏洞（例如鉴权绕过、任意文件读取、SQL 注入、敏感信息泄漏等），请**不要**在公开 issue 里讨论或贴出细节，改用下面的私密方式联系：

- **首选**：GitHub 的 **Private vulnerability reporting**（私密漏洞上报）通道——仓库页面 → Security → Report a vulnerability。它会把漏洞私密通知给维护者，修复后我再决定是否申请 CVE、什么时候公开。
- **备用**：如果该通道未启用，请通过提交历史里作者邮箱私下联系。

上报时尽量包含：

- 漏洞的类型和影响范围
- 复现步骤或 PoC（能最小复现最好）
- 你的版本和部署方式（Docker Compose 还是本地编译）

## 处理流程

1. 收到上报后 48 小时内确认收到。
2. 修复并验证前不公开任何细节。
3. 修复上线后，通过版本发布 / commit 记录披露，并在此文件补充说明。

## 已知安全边界

- 本项目是**自部署**应用：`DeepSeek API Key`、数据库密码、超管密码都只放在服务器 `.env` 或数据库中，**不进入 git**（详见 `DEPLOYMENT.md`）。
- 仓库已配置 gitleaks 扫描（`.github/workflows/gitleaks.yml`），在 push/PR 时兜底拦截密钥泄漏。
- 如果发现提交历史里疑似有密钥，请按上面方式私密上报，而不是在 issue 里贴出来。
